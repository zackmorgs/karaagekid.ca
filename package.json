{
  "name": "karaagekid",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "firebase:login": "npx firebase-tools login",
    "firebase:init": "npx firebase-tools init hosting",
    "deploy": "npm run build && npx firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "astro": "^6.1.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "sass": "^1.99.0"
  }
}
